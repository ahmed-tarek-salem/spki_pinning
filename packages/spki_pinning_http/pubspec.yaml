name: spki_pinning_http
description: >-
  package:http binding for spki_pinning: SSL/TLS certificate pinning of the
  SPKI public key via a routing Client. Fail-closed, with backup pins.
version: 0.1.0
repository: https://github.com/ahmed-tarek-salem/spki_pinning
topics: [ssl-pinning, certificate-pinning, security, tls, network]
environment:
  sdk: ">=3.4.0 <4.0.0"
false_secrets:
  - /test/fixtures/*.key.pem
dependencies:
  http: ^1.2.0
  spki_pinning: ^0.1.0
dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
