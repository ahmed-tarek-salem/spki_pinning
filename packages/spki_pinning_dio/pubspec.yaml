name: spki_pinning_dio
description: >-
  Dio binding for spki_pinning: SSL/TLS certificate pinning of the SPKI
  public key via a routing HttpClientAdapter. Fail-closed, with backup pins.
version: 0.1.0
repository: https://github.com/ahmed-tarek-salem/spki_pinning
topics: [ssl-pinning, certificate-pinning, security, tls, network]
environment:
  sdk: ">=3.4.0 <4.0.0"
false_secrets:
  - /test/fixtures/*.key.pem
dependencies:
  dio: ^5.4.0
  spki_pinning: ^0.1.0
dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
