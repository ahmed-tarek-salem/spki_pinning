name: spki_pinning_example
description: Flutter demo for spki_pinning, live pin probing, pinned and rejected requests, and multi-host routing.
publish_to: "none"
version: 1.0.0
environment:
  sdk: ">=3.4.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  spki_pinning:
    path: ../
  spki_pinning_dio:
    path: ../../spki_pinning_dio
dev_dependencies:
  flutter_lints: ^4.0.0
flutter:
  uses-material-design: true
